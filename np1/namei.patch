--- a/fs/namei.c
+++ b/fs/namei.c
@@ -3900,6 +3900,10 @@ struct file *do_filp_open(int dfd, struct filename *name, const struct open_fla
 	struct filename *filename = name;
 	struct file *filp;
 
+#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
+	struct filename *fake_filename = NULL;
+#endif
+
 	filp = path_openat(&nd, op, flags);
 	if (IS_ERR(filp))
 		return filp;